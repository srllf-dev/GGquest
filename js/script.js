// GGquest — lógica principal
// Por enquanto só confirmando que o JS está conectado ao HTML.

document.addEventListener("DOMContentLoaded", () => {
  const statusEl = document.getElementById("status-check");
  statusEl.textContent = "✅ HTML, CSS e JS conectados. Bora construir!";
});
